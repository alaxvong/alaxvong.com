# alaxvong.com

Personal landing page — a single static `index.html`. No build step.

## Local preview

Open `index.html` directly, or serve the folder:

```bash
npx serve .
```

Then visit http://localhost:8000.